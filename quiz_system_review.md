# סקר ארכיטקטורה וקוד - מערכת החידונים (Quiz System)

עברתי בקפידה על קובצי ה-C# וה-SQL של מערכת החידונים שיצרת. הארכיטקטורה הכללית טובה מאוד והחלוקה לשכבות (Controllers, BL, DAL) ברורה ומסודרת.
יחד עם זאת, מצאתי מספר נקודות קריטיות שכדאי לטפל בהן לפני שאתה מריץ את יצירת הטבלאות בסביבת הייצור, במיוחד בתחומי האבטחה, הביצועים ותקינות הנתונים.

## 🔒 1. אבטחה (Security)
> [!CAUTION]
> **זיהוי משתמשים דרך ה-Query String:** ב-Controllers רבים (למשל `[FromQuery] int userId`), אתה מקבל את זהות המשתמש כפרמטר מה-Client. הדבר מהווה פרצת אבטחה חמורה – כל אחד יכול לשלוח בקשת API עם `userId=1` (או כל מזהה אחר) ולערוך או למחוק חידונים של משתמשים אחרים.
> *המלצה:* השתמש במערכת אותנטיקציה כגון **JWT Tokens** או סשנים, חלץ את זהות המשתמש ישירות מתוך ה-Token בשרת (למשל דרך ה-`User.Claims`), ואל תסמוך על פרמטר שנשלח ב-URL.


## ⚡ 2. ביצועים ויעילות מסד הנתונים (Performance)
> [!IMPORTANT]
> **קריאות למסד הנתונים בתוך לולאות (N+1 Problem):**
> בפונקציות `CreateQuiz` ו-`CreateAttempt` ב-`DBServiceQuiz`, מתבצעת לולאת `foreach` שקוראת ל-SP של הוספת שאלה/תשובה עבור כל פריט. כל קריאה כזו רצה בנפרד, מה שיעמיס מאוד על השרת ככל שהחידונים יהיו ארוכים יותר וכמות המשתמשים תגדל.
> *המלצה:* ניתן לייעל זאת על ידי שליחת כל מערך השאלות בבת אחת למסד הנתונים באמצעות **JSON** (ופרסור עם `OPENJSON` ב-SQL) או באמצעות **Table-Valued Parameters (TVP)**, ולבצע את הלולאה ברמת ה-SQL.

> [!WARNING]
> **ניהול חיבורים (Connection Management):** במידה והאובייקט `con` שמגיע מ-`DBServiceBase` מוגדר כ-`static`, ברגע שיותר ממשתמש אחד יפנה למערכת במקביל, השרת יקרוס ויזרוק שגיאות (Concurrency Issues).
> *המלצה:* הימנע ממשתנים סטטיים לניהול Connections ברמת השרת. תמיד השתמש ב-`using (SqlConnection con = new SqlConnection(ConnectionString))` באופן פרטני עבור כל פונקציה.

## 🏛️ 3. תקינות נתונים וטרנזקציות (Data Integrity)
> [!CAUTION]
> **היעדר SQL Transactions:** במבנה הנוכחי של השמירה, אם הכנסת כותרת החידון תצליח אך תתרחש שגיאה באחת מהשאלות (למשל מחרוזת ארוכה מדי), החידון יישאר במסד הנתונים במצב "חצי-אפוי", ויהיו בו שאלות חסרות. הדבר נכון גם לגבי הגשת מבחן (`CreateAttempt`).
> *המלצה:* עטוף את רצף הפעולות ב-`SqlTransaction` ברמת ה-C#, או פתור זאת באמצעות פניה לפרוצדורה אחת שעוטפת הכל ב-`BEGIN TRAN` / `COMMIT` ברמת ה-SQL.

> [!NOTE]
> **מחיקת משתמש (Cascade Delete):** בטבלאות שיצרת, השתמשת נכון ב-`ON DELETE CASCADE` ברוב המקומות. עם זאת, בטבלת `FP_Quizzes2026` למפתח הזר `CreatorId` חסר ה-Cascade:
> `CONSTRAINT FK_Quizzes_Creator FOREIGN KEY (CreatorId) REFERENCES FP_Users2026(Id)`
> *משמעות:* במידה ותנסה למחוק משתמש מהמערכת (למשל מנהל שמסיר יוזר), מסד הנתונים יזרוק שגיאה וימנע את המחיקה אם יש לאותו יוזר חידונים. אם אתה רוצה שהחידונים שלו ימחקו איתו, כדאי להוסיף `ON DELETE CASCADE` גם שם.
